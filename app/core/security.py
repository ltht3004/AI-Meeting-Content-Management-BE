# Password hashing and token generation helpers
def verify_password(plain_password: str, hashed_password: str) -> bool:
    return False

def get_password_hash(password: str) -> str:
    return ""

def create_access_token(subject: str) -> str:
    return ""
